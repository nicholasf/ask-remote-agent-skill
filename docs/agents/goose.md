# Goose Agent Setup

[Goose](https://block.goose.ai/) is an optional agent runtime for peer mode.

Setup guide pending — installation session not yet completed.

Once installed, this guide will cover:
1. Installing Goose on the remote node
2. Configuring it to use the local LLM endpoint
3. Enabling its HTTP API
4. Registering the node in `topology.md` and `$SKILLS_HOME/.env`
5. Verifying end-to-end with `agent.py --peer-node <hostname>`
